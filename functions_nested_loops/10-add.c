#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers
 * @x: the number is checker
 * @y: the number is checker
 *
 * Return: Always 0 (Success)
 */
int add(int x, int y)
{
	return (x + y);
}
